const path = require('path')

module.exports = {
  siteMetadata: {
    title: 'Demo Blog',
    description: 'with demo data',
    config: {
      sideBarWidth: 240
    }
  },
  plugins: ['@pauliescanlon/gatsby-theme-gatstats']
}
