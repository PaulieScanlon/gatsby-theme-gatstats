import * as React from 'react'

import { Spinner } from './Spinner'

export default {
  title: 'Spinner',
  parameters: {
    component: Spinner,
    componentSubtitle: 'Spinner is...'
  }
}

export const usage = () => <Spinner />
